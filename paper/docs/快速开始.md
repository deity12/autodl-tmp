# 快速开始指南

> 5分钟上手 QL-MATCC-GNN 项目

## 前置条件

### 硬件要求
- **GPU**: 48GB显存（vGPU/RTX 3090/A6000等）
- **CPU**: 8核+ (推荐12核)
- **RAM**: 32GB+ (推荐90GB)
- **存储**: 100GB+ 可用空间

### 软件环境
```bash
OS: Ubuntu 22.04
Python: 3.10
CUDA: 11.8
PyTorch: 2.1.2+
```

### 默认配置说明

**论文复现默认配置**：
- ✅ **LLM默认启用**: 使用Qwen2.5-14B提取语义关系
- ✅ **模型配置**: n_embd=256, n_layers=3, gnn_embd=64
- ✅ **训练配置**: batch_size=512, epochs=20
- ✅ **数据采样**: 200条/股票 (高质量图谱)

**关于“更大配置/48GB优化”**：
当前 `training/train_full.py` / `training/train_ablation.py` **未提供 CLI 超参**（没有 `--n_embd/--batch_size/...` 参数）。
如需更大模型/更大 batch/更多 epoch，请直接修改脚本内的 `CONFIG` / `BASE_CONFIG` 字典（README 已说明位置）。

## 第一步：环境配置

### 1.1 安装 PyTorch

```bash
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
```

### 1.2 安装其他依赖

```bash
pip install pennylane pandas numpy scikit-learn matplotlib tqdm akshare
```

### 1.3 验证安装

```python
python -c "import torch; print(f'CUDA可用: {torch.cuda.is_available()}')"
python -c "import pennylane as qml; print(f'PennyLane版本: {qml.__version__}')"
```

## 第二步：数据准备

### 2.1 数据目录结构

```
paper/data/
├── raw/
│   └── FNSPID/
│       ├── full_history/      # 个股CSV文件
│       │   ├── AAPL.csv
│       │   ├── MSFT.csv
│       │   └── ...
│       ├── nasdaq_exteral_data.csv  # 新闻数据（23GB）
│       └── SP500_Index.csv    # S&P 500指数（可自动下载）
└── processed/                 # 自动生成
```

### 2.2 下载S&P 500指数（可选）

如果 `SP500_Index.csv` 不存在：

```bash
cd paper
python -m dataProcessed.download_market_index
```

**手动方式**：也可从 Yahoo Finance 下载，格式为：
```csv
Date,Market_Close,Market_Vol
2019-01-02,2510.03,3682930000
...
```

## 第三步：数据处理

### 3.1 ETL处理（合并股价+新闻）

```bash
python -m dataProcessed.etl
```

**输出**：
- `data/processed/Stock_Prices.csv` (~500MB)
- `data/processed/Stock_News.csv` (~2GB)

**耗时**: 约5-10分钟

### 3.2 数据对齐（计算特征）

```bash
python -m dataProcessed.align
```

**输出**：
- `data/processed/Final_Model_Data.csv` (~800MB)

**耗时**: 约2-5分钟

### 3.3 构建图谱（S&P 500成分股）

```bash
python -m dataProcessed.build_graph
```

**输出**：
- `data/processed/Graph_Adjacency.npy`
- `data/processed/Graph_Adjacency_tickers.json`（强烈建议保留：用于校验图谱节点顺序，避免“静默错位”）

**耗时**:
- LLM 模式（默认）：通常为数小时量级（与 batch/推理参数/GPU 吞吐相关）
- 规则模式：约1-3分钟

**高级选项**：
```bash
# 强制使用 LLM 提取关系（需要大显存）
python -m dataProcessed.build_graph --use_llm

# 强制使用规则匹配（更快，显存占用小）
python -m dataProcessed.build_graph --no_llm

# 使用全量股票（不推荐）
python -m dataProcessed.build_graph --all_stocks

# 自定义采样参数
python -m dataProcessed.build_graph --max_per_ticker 100 --max_total 100000
```

## 第四步：训练模型

### 4.1 训练完整模型

```bash
python -m training.train_full
```

**输出**：
- `outputs/checkpoints/best_model_full.pth`
- `outputs/logs/training_losses_full.json`
- `outputs/figures/training_curve_full.png`

**耗时**: 约2-4小时（20 epochs）

### 4.2 运行消融实验

```bash
python -m training.train_ablation
```

**输出**：
- `outputs/checkpoints/best_model_*.pth`（3个消融模型）
- `outputs/logs/losses_*.json`（每个实验一个）
- `outputs/results/ablation_results_summary.csv`
- `outputs/figures/ablation_comparison.png`

**耗时**: 约6-12小时（3个实验 × 20 epochs）

## 第五步：评估模型

### 5.1 统一评估

```bash
python -m evaluation.evaluate_all
```

**输出**：
- `outputs/results/evaluation_overall.csv`
- `outputs/results/evaluation_by_volatility.csv`
- `outputs/figures/evaluation_comparison.png`

### 5.2 分组评估

```bash
python -m evaluation.evaluate_by_group
```

**输出**：
- `outputs/results/group_evaluation_*.csv`

## 常见问题

### Q1: CUDA out of memory

**解决方案**：
```python
# 在 train_full.py 中调整参数
batch_size = 256  # 从512降到256
n_embd = 128      # 从256降到128
```

### Q2: build_graph.py 运行很慢

**原因**: 默认启用 LLM 模式，需要加载 14B 参数模型并对大量新闻做推理

**解决方案**: 如只想先跑通流程/显存不足，可切换到规则模式
```bash
python -m dataProcessed.build_graph --no_llm
```

### Q3: 数据文件找不到

**检查路径**：
```bash
ls data/raw/FNSPID/full_history/ | head -5
ls data/raw/FNSPID/nasdaq_exteral_data.csv
```

**确保**：
- 股票CSV文件在 `full_history/` 目录下
- 文件名格式为 `TICKER.csv` (如 `AAPL.csv`)

### Q4: 图谱连接率太低

**检查统计输出**：
```
有连接的股票数: 150 / 300 (50.0%)
```

**优化方案**：
```bash
# 增加每股票采样数
python -m dataProcessed.build_graph --max_per_ticker 100

# 或使用LLM模式（更准确）
python -m dataProcessed.build_graph --use_llm
```

### Q5: 训练loss不下降

**可能原因**：
1. 学习率过大/过小
2. 数据异常值未处理
3. 模型初始化问题

**解决方案**：
```python
# 1. 调整学习率
lr = 1e-4  # 从3e-4降低

# 2. 检查数据
python -c "
import pandas as pd
df = pd.read_csv('data/processed/Final_Model_Data.csv')
print(df.describe())
"

# 3. 使用更小的模型
n_embd = 128
n_layers = 2
```

## 下一步

- 阅读 [模块说明](模块说明.md) 了解各个文件的作用
- 阅读 [训练与评估](训练与评估.md) 了解详细的训练技巧
- 阅读 [改进方案说明](改进方案说明.md) 了解优化历程

## 一键运行（48GB优化版）⚡

### 推荐方式: 使用优化脚本

```bash
cd /root/autodl-tmp/paper

# 创建screen会话（防止SSH断连）
screen -S ql_matcc

# 运行优化脚本（已针对48GB显存优化）
./run_optimized.sh

# 分离会话: Ctrl+A, D
# 重新连接: screen -r ql_matcc
# 查看所有会话: screen -ls
```

### 手动逐步运行

```bash
cd /root/autodl-tmp/paper

# Step 1: 数据处理
python -m dataProcessed.etl
python -m dataProcessed.align
python -m dataProcessed.build_graph --use_llm  # LLM模式 (60-90分钟)

# Step 2: 训练完整模型
python -m training.train_full  # 120-150分钟 (20 epochs)

# Step 3: 消融实验（可选）
python -m training.train_ablation  # 360-450分钟 (3×20 epochs)

# Step 4: 评估
python -m evaluation.evaluate_all
python -m evaluation.evaluate_by_group
```

---

**预计时间** (默认配置):
- 🔄 数据处理: 70-100分钟（含LLM图谱）
- 🎓 完整模型: 120-150分钟 (20 epochs)
- 🧪 消融实验: 360-450分钟 (3个实验 × 20 epochs)
- 📊 评估: 10分钟
- **总计**: ~9-12小时

**注意事项**:
1. ✅ 使用 `screen` 或 `tmux` 防止SSH断连
2. ✅ 磁盘空间充足（至少50GB可用）
3. ✅ 首次运行会下载Qwen2.5-14B模型（~26GB）
4. ✅ 定期运行 `nvidia-smi` 监控显存使用
